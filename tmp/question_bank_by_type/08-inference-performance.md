# 推理部署与性能优化

频次：

- `推理部署 / 性能优化 / vLLM`：`44`
- 覆盖面经数：`25`

核心问题：

1. 端到端延迟由哪些环节组成？
2. `vLLM`、`KV Cache`、`Batching`、`Streaming` 分别解决什么问题？
3. 显存不足时有哪些优化手段？
4. 量化、缓存、路由、剪枝各适合什么问题？
5. 为什么线上一定要有限流、超时、重试、熔断？
6. 模型路由如何平衡效果、成本、时延？
7. Prompt 裁剪和上下文压缩为什么也是性能优化？

高频追问：

1. `FlashAttention`、`PagedAttention` 大概解决什么瓶颈？
2. 延迟过高时如何判断是检索慢、工具慢还是推理慢？
3. 什么时候值得做异步化和任务队列化？

回答抓手：

- 先做链路拆解再讲优化
- 优化不只盯模型，还要看检索、工具、后处理
- 一定讲观测，否则无法定位瓶颈
