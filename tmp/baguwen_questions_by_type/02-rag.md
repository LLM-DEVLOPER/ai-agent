# RAG

## 1. 什么是 RAG？

1. 什么是 RAG？
2. RAG 的核心思想是什么？
3. 为什么 RAG 可以被理解成“先查资料，再回答”？
4. RAG 到底在解决大模型的什么问题？
5. 为什么 RAG 会成为 LLM 应用开发里的核心基础设施？
6. RAG 的核心价值通常可以概括为哪几个方面？

## 2. 为什么需要 RAG？

1. 为什么大模型应用里经常需要 RAG？
2. RAG 想解决的核心痛点是什么？
3. RAG 在知识时效性上解决了什么问题？
4. RAG 在幻觉治理上解决了什么问题？
5. RAG 在私有知识利用上解决了什么问题？
6. RAG 为什么能提升可追溯性和可解释性？
7. RAG 能不能 100% 解决幻觉问题？
8. 在什么情况下接了 RAG 仍然会产生幻觉？

## 3. 传统 RAG 的边界和痛点在哪里？

1. 传统 RAG 的核心边界主要体现在哪些方面？
2. 为什么基础 RAG 一旦遇到复杂问题就容易失效？
3. 为什么传统 RAG 很容易卡在“检索到了，但还是答不好”？
4. 为什么多跳问题、复杂推理问题、结构化决策问题对传统 RAG 不友好？
5. 什么情况下基础 RAG 已经不够，需要升级链路？
6. RAG 通常可以大致分成哪些类型？

## 4. RAG 的完整链路是什么？

1. 一个完整的 RAG 系统通常包含哪些阶段？
2. 为什么 RAG 一般要拆成离线索引和在线检索生成两大阶段？
3. 离线阶段通常在做什么？
4. 在线阶段通常在做什么？
5. 从原始文档到最终回答，中间经过了哪些关键步骤？
6. 一个典型的 RAG 结构图通常应该怎么拆？
7. RAG 链路里，哪几个环节最容易决定最终效果上限？

## 5. 离线索引阶段为什么重要？

1. 为什么说离线索引质量决定了 RAG 的上限？
2. 离线阶段通常包括哪些步骤？
3. 数据清洗为什么是 RAG 的第一步？
4. 原始文档接入时最常见的脏数据问题有哪些？
5. 为什么很多 RAG 问题本质上是数据治理问题，而不是模型问题？
6. 索引构建为什么不能只理解成“把文本转成向量”？

## 6. Chunk 为什么是 RAG 的基石？

1. 为什么 chunk 是 RAG 最关键的基础问题之一？
2. chunk 太小会有什么问题？
3. chunk 太大会有什么问题？
4. 为什么分块策略本质上是在平衡语义完整性和检索精度？
5. 常见的 chunk 策略有哪些？
6. 递归分块为什么常常比简单定长切分更稳？
7. chunk overlap 在解决什么问题？
8. Markdown、表格、代码、FAQ 文档的 chunk 策略为什么不一样？
9. 父子块、small-to-big 这类策略在解决什么问题？
10. `chunk size` 通常应该根据哪些因素来定？

## 7. Embedding 在 RAG 里扮演什么角色？

1. 什么是 embedding？
2. embedding 模型在 RAG 中到底起什么作用？
3. embedding 模型为什么决定了召回上限？
4. 选择 embedding 模型时应该看哪些维度？
5. 通用 embedding 和领域 embedding 应该怎么选？
6. 为什么不能只看榜单选 embedding 模型？
7. 什么情况下需要对 embedding 模型做微调？
8. embedding 微调想解决的通常是什么问题？
9. embedding 模型和生成模型微调的目标有什么区别？

## 8. 检索阶段到底在做什么？

1. 检索阶段的核心目标是什么？
2. 为什么召回层强调“快和全”？
3. 检索方式一般有哪些？
4. 关键词检索和向量检索各自适合什么场景？
5. 为什么很多生产级 RAG 最终都会用混合检索？
6. BM25 在 RAG 里为什么仍然重要？
7. 混合检索到底在弥补什么问题？
8. 元数据过滤为什么是企业级 RAG 的刚需？
9. 多租户、权限控制、时间过滤通常放在检索链路的哪一层？

## 9. 向量库与索引该怎么理解？

1. 向量数据库在 RAG 中到底负责什么？
2. 向量库应该如何选型？
3. `pgvector`、`FAISS`、`Milvus`、`Qdrant`、`Weaviate` 这类方案大致怎么取舍？
4. 向量库独立部署和嵌在主库里，各自适合什么场景？
5. `IVF` 和 `HNSW` 的区别是什么？
6. 索引构建速度、检索速度、召回效果通常怎么权衡？
7. 大规模索引更新为什么是工程难点？
8. 增量更新和全量重建分别适合什么情况？

## 10. 为什么很多 RAG 都要做 Re-rank？

1. 为什么召回之后还要做 rerank？
2. 召回层和重排层的职责边界是什么？
3. reranker 在解决什么问题？
4. 为什么 embedding 相似度高，不代表最终相关性一定高？
5. Cross-Encoder 类 reranker 为什么通常更准？
6. rerank 的代价是什么？
7. rerank 后一般返回几个块比较合适？
8. topk 和 topn 应该怎么定？
9. rerank 截断阈值为什么经常要靠实验确定？

## 11. 生成阶段为什么不是“把检索结果直接拼进去”？

1. 检索完成后，生成阶段还要解决什么问题？
2. 为什么上下文构造会直接影响最终答案质量？
3. Prompt 中通常应该包含哪些部分？
4. 为什么必须明确要求模型“基于给定上下文回答”？
5. 为什么很多 RAG 系统都需要拒答机制？
6. 检索到的上下文应该按什么顺序放进 Prompt？
7. 为什么重要上下文的位置会影响模型利用效果？
8. 上下文过多时，为什么不能简单全塞进去？
9. 上下文压缩和上下文排序分别在解决什么问题？

## 12. RAG 最常见的问题通常出在哪些链路环节？

1. 为什么很多 RAG 问题不是单点问题，而是链路协同问题？
2. 检索到了相关内容，但答案还是不对，通常问题出在哪里？
3. 如果召回率低，应该优先检查哪些环节？
4. 如果召回结果看起来没问题，但模型没用上，通常该排查什么？
5. 如果生成结果离上下文越来越远，通常是哪一层没管住？
6. 为什么很多业务里“召回有了，效果却没提升”？
7. RAG 系统里最常见的 bad case 一般怎么分类？

## 13. RAG 应该怎么评测？

1. RAG 应该怎么做评测？
2. 为什么 RAG 评测不能只看最终答案？
3. 召回效果应该怎么评估？
4. 重排效果应该怎么评估？
5. 生成效果应该怎么评估？
6. Faithfulness、Relevance、Context Utilization 分别在评什么？
7. 为什么 RAGAS 这类框架会流行？
8. 自动化评测和人工评测应该怎么配合？
9. 如何通过 bad case 回流持续优化 RAG？

## 14. RAG 优化通常从哪几层下手？

1. RAG 优化通常可以从哪几层切入？
2. 数据清洗优化通常在解决什么问题？
3. chunk 策略优化通常在解决什么问题？
4. embedding 优化通常在解决什么问题？
5. 检索策略优化通常在解决什么问题？
6. rerank 优化通常在解决什么问题？
7. Prompt 和上下文构造优化通常在解决什么问题？
8. 查询改写、查询扩展、多路召回各自适合什么场景？
9. 多跳检索和上下文压缩分别适合什么场景？

## 15. RAG 和 Fine-tuning 到底怎么选？

1. RAG 和 Fine-tuning 的核心区别是什么？
2. 什么时候问题的本质是“知识问题”？
3. 什么时候问题的本质是“能力问题”？
4. 什么情况下优先用 RAG？
5. 什么情况下优先用微调？
6. 为什么很多生产系统最后都会是 `RAG + Fine-tuning` 的组合？
7. 高准确率、高合规场景里 RAG 和微调通常该从哪些维度取舍？

## 16. Agent 和 RAG 的关系到底是什么？

1. Agent 和 RAG 的关系是什么？
2. RAG 是不是 Agent 的一部分？
3. Agentic RAG 在解决什么问题？
4. 普通 RAG 和 Agent 增强型 RAG 的区别是什么？
5. 什么时候只做 RAG 就够了？
6. 什么时候必须让 Agent 参与检索、规划和多步推理？
7. 为什么很多复杂问答最终会从“基础 RAG”演化成“Agentic RAG”？

## 17. 高级 RAG 方案在解决什么问题？

1. GraphRAG 在解决什么问题？
2. GraphRAG 适合什么场景？
3. GraphRAG 的难点通常在哪？
4. GraphRAG 的增量更新为什么会更难？
5. 多路召回在解决什么问题？
6. 多查询改写在解决什么问题？
7. 多跳检索在解决什么问题？
8. 为什么高级 RAG 方案通常不是默认就值得上？

## 18. RAG 项目深挖

1. RAG 项目的全链路通常应该怎么设计？
2. 项目里为什么要做 RAG，而不是只靠 Prompt 或微调？
3. 项目里的 chunk 策略通常应该怎么定？
4. embedding 模型和 reranker 模型通常该怎么选？
5. 向量库通常为什么会选成当前方案？
6. 项目里的检索链路通常应该怎么设计？
7. 项目里为什么会引入 rerank？
8. 项目里一般怎么评估 RAG 效果？
9. 线上最难处理的 RAG bad case 通常有哪些？
10. 重做一次这个 RAG 系统时，通常最值得优先改哪一层？
