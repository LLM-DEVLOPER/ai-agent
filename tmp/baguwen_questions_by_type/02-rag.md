# RAG

## 1. 核心概念与原理

1. 什么是 RAG，它的核心思想是什么？
2. 为什么大模型应用经常需要 RAG，而不是只靠 Prompt？
3. RAG 主要在解决大模型的哪些痛点？
4. RAG 能不能真正解决幻觉问题，它的边界在哪里？
5. 什么问题适合 RAG，什么问题不适合 RAG？

## 2. RAG 的完整工作流程

1. 请从整体上描述一个完整的 RAG 系统通常包含哪些阶段？
2. 为什么 RAG 一般要拆成离线索引和在线检索生成两大阶段？
3. 从原始文档到最终回答，中间通常会经过哪些关键步骤？
4. 在整条 RAG 链路里，哪几个环节最容易决定最终效果上限？

## 3. 离线索引阶段：数据治理、Chunk 与 Embedding

1. 离线索引阶段通常在做什么，为什么它决定了 RAG 的上限？
2. 数据清洗为什么往往是 RAG 的第一步，常见脏数据问题有哪些？
3. 为什么 chunk 是 RAG 最关键的基础问题之一，太大和太小分别会带来什么问题？
4. 常见的 chunk 策略有哪些，递归分块、overlap、父子块、small-to-big 分别适合什么场景？
5. embedding 在 RAG 里扮演什么角色，选择 embedding 模型时通常看哪些维度？
6. 什么情况下值得对 embedding 模型做微调？

## 4. 在线检索阶段：召回、向量库与重排

1. 检索阶段的核心目标到底是什么，为什么召回层通常强调“快和全”？
2. BM25、向量检索、混合检索分别适合什么场景，为什么生产级 RAG 往往会走到混合检索？
3. 向量库在 RAG 中到底负责什么，选型时通常应该看哪些维度？
4. `pgvector`、`FAISS`、`Milvus`、`Qdrant` 这类方案大致应该怎么取舍？
5. 为什么很多 RAG 都要在召回之后再做 rerank，召回层和重排层的职责边界是什么？
6. topk、topn、截断阈值这类参数通常应该怎么定？

## 5. 生成阶段：上下文构造、Prompt 与回答约束

1. 为什么生成阶段不是“把检索结果直接拼进去”这么简单？
2. 上下文排序、压缩、去重分别在解决什么问题？
3. 一个 RAG Prompt 里通常应该包含哪些部分？
4. 为什么很多 RAG 系统都需要拒答、引用和答案约束？
5. 为什么会出现“明明查到了，但还是答不好”？

## 6. 评测、排障与优化闭环

1. 为什么很多 RAG 问题不是单点问题，而是链路协同问题？
2. 如果召回率低，通常应该优先检查哪些环节？
3. 如果召回结果看起来没问题，但模型没用上，通常该排查什么？
4. RAG 系统里的 bad case 通常应该怎么分类？
5. 为什么 RAG 不能只看最终答案来做评测？
6. 一个成熟的 RAG 系统通常应该怎么建立评测和优化闭环？

## 7. 高级方案与系统工程

1. 什么是多查询改写、多路召回，它们分别在解决什么问题？
2. 什么是多跳检索，它更适合什么类型的问题？
3. 什么是 Agentic RAG，它和普通 RAG 的区别是什么？
4. GraphRAG 更适合什么场景，为什么它不是默认方案？
5. 在真实系统里，索引更新、增量构建、成本和性能通常怎么权衡？

## 8. RAG 与 Fine-tuning 的取舍

1. RAG 和 Fine-tuning 的核心区别是什么，通常应该怎么取舍？
2. 什么是“知识问题”，什么是“能力问题”，两者分别更适合用什么方案解决？
3. 什么情况下更适合只做 RAG，什么情况下需要 RAG 和 Fine-tuning 结合？

## 9. 项目深挖

1. 一个 RAG 项目的全链路通常应该怎么设计？
2. 项目里的 chunk、embedding、reranker、向量库通常应该怎么选？
3. 项目里一般怎么评估 RAG 的效果？
4. 线上最难处理的 RAG bad case 通常有哪些？
5. 如果重做一次当前 RAG 系统，最值得优先改哪一层，为什么？
