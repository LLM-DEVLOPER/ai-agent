# 记忆、上下文工程、长上下文与 KV Cache

## 1. 上下文工程与记忆系统的定义和边界

1. 为什么在 Agent 和 LLM 应用里，记忆和上下文工程通常要放在一起讨论？
2. 什么是上下文工程，什么是记忆系统？
3. `memory`、`context`、`history`、`state` 这几个概念的边界分别是什么？
4. 为什么很多 AI 应用的问题，本质上不是模型能力问题，而是上下文构造问题？

## 2. 上下文窗口、Token 预算与上下文构造

1. 模型的上下文窗口是什么意思，上下文窗口越大越好吗？
2. Token 预算通常应该如何分配给系统提示词、历史对话、检索结果、工具结果和用户输入？
3. 一个完整的上下文通常由哪些部分组成？
4. `system message`、`developer message`、`user message` 的优先级通常应该怎么理解？
5. 为什么很多系统都会尽量保持 `system message` 和固定前缀稳定不变？
6. 多轮对话时上下文通常应该按什么顺序组织？
7. 工具结果、检索结果进入上下文时，通常应该如何做去重、排序、裁剪和摘要？

## 3. 多轮对话、历史管理与上下文压缩

1. 多轮对话时上下文应该如何管理？
2. 对话历史应该全部保留、窗口滑动，还是分层存储？
3. 为什么多轮对话里特别容易出现话题漂移、约束丢失和状态混乱？
4. 上下文裁剪、上下文压缩、历史摘要分别在解决什么问题？
5. 滑动窗口、摘要记忆、结构化状态提取分别适合什么场景？
6. 为什么直接截断历史往往会伤害效果？

## 4. 长上下文与 Long Context

1. 长上下文能力在应用层到底能解决什么问题？
2. 长上下文模型的实际效果为什么常常低于想象？
3. “Lost in the Middle” 是什么，它为什么重要？
4. 长上下文场景下，信息排布顺序通常应该怎么设计？
5. 长上下文什么时候应该配合检索，而不是直接全塞进去？
6. 长上下文和 RAG、摘要压缩分别是什么关系？

## 5. 记忆系统设计

1. Agent 的记忆系统通常应该怎么设计？
2. 长期记忆和短期记忆分别怎么理解？
3. 混合记忆架构通常应该怎么设计？
4. 用户画像记忆、偏好记忆、事实记忆、任务记忆为什么要拆开？
5. 哪些信息适合写入长期记忆，哪些信息不应该写？

## 6. 长期记忆的写入、召回、更新与淘汰

1. 长期记忆如何写入、召回、更新、删除和淘汰？
2. 记忆写入的触发条件通常应该怎么定？
3. 记忆衰退、记忆覆盖、冲突记忆通常应该怎么处理？
4. 用户偏好变化后，旧记忆通常应该如何修正？
5. 召回长期记忆时，如何控制误召回？

## 7. 外部记忆、RAG 与 Memory Search

1. 记忆和 RAG 的边界是什么？
2. 什么是外部记忆机制，`memory_search` 这类能力通常怎么接？
3. 什么时候应该用记忆检索，什么时候应该用知识库检索？
4. 用户私有记忆和公共知识库为什么必须严格区分？
5. 为什么很多 Agent 同时需要“会话记忆 + 用户记忆 + RAG 知识库”三套机制？

## 8. Session、线程隔离与并发安全

1. 多用户并发下，memory 通常如何做隔离？
2. 会话级状态、用户级状态、任务级状态应该如何隔离？
3. 一个用户同时开多个会话时，记忆通常应该如何路由？
4. 多设备同步和高并发下，状态一致性通常应该怎么保证？
5. 如果同一条记忆被多个流程同时更新，如何避免覆盖？

## 9. KV Cache、Prefix Cache 与上下文复用

1. `KV Cache` 是什么，它为什么能提升推理速度？
2. `KV Cache` 节省的是哪一部分计算？
3. 为什么 `KV Cache` 和应用层 memory 不能混为一谈？
4. `prefix cache` 是什么，它和 `KV Cache` 的关系是什么？
5. 为什么固定的 `system message`、工具 schema、固定前缀特别适合做 `prefix cache`？
6. 哪些上下文部分通常可以稳定复用，哪些部分几乎不能复用？
7. `KV Cache`、`prefix cache`、会话历史持久化三者分别解决什么问题？
8. 为什么 `KV Cache` 往往会继续追到上下文工程与应用层状态设计？

## 10. 工具调用结果、上下文污染与工程落地

1. 工具调用结果为什么特别容易污染上下文？
2. 工具返回的长文本通常应该如何进入上下文？
3. ReAct 轨迹、tool message、observation 应该如何分层保留？
4. 多工具串行调用时，如何避免上下文越来越脏？
5. 当前端、后端、Agent 编排层一起参与时，上下文职责一般应该怎么拆？

## 11. 评测、问题定位与项目深挖

1. 如何评估上下文工程是否真的有效？
2. 如何评估记忆系统是否真的提升了效果？
3. 记忆命中率高，但回答效果差，可能是什么原因？
4. 如何定位是“记忆没召回”，还是“召回了但没被模型利用”？
5. 如果让你设计一个带长期记忆的 Agent，你会怎么做？
6. 如果一个系统既要快、又要记住用户、还要避免上下文污染，你会怎么设计？
