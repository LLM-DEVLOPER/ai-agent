# 记忆、上下文工程、长上下文与 KV Cache

## 1. 记忆与上下文的总起手题

1. 为什么在 Agent 和 LLM 应用里，记忆和上下文工程通常要放在一起讨论？
2. 什么是上下文工程？
3. 什么是记忆系统？
4. 为什么长上下文窗口不能直接替代记忆系统？
5. 为什么很多 AI 应用的问题，本质上不是模型能力问题，而是上下文构造问题？
6. `memory`、`context`、`history`、`state` 这几个概念的边界分别是什么？
7. 记忆、上下文、状态管理这三者分别解决什么问题？
8. 工作记忆、会话记忆、长期记忆、用户画像记忆分别是什么？
9. 对话历史、任务状态、工具结果、外部知识分别应该放在哪一层？
10. 什么场景更依赖上下文工程，什么场景更依赖记忆系统？

## 2. 上下文窗口与 Token 预算

1. 模型的上下文窗口是什么意思？
2. 上下文窗口越大越好吗？
3. 为什么长上下文会带来效果下降，而不是一定更好？
4. 上下文窗口受限时，最先应该优化哪一层？
5. Token 预算应该怎么分配给系统提示词、历史对话、检索结果、工具结果、用户输入？
6. 当上下文接近阈值时，应该先裁剪什么？
7. 为什么“把所有历史都塞进去”通常是错误做法？
8. 多轮对话里，哪些内容必须保留在窗口内，哪些内容可以外置？
9. 如何判断当前问题需要短上下文、长上下文，还是外部记忆？
10. 上下文爆炸一般是由哪些模块共同造成的？
11. 系统提示词过长会带来什么问题？
12. 工具调用链路为什么容易把上下文打爆？

## 3. 上下文组织与构造

1. 一个完整的上下文通常由哪些部分组成？
2. 系统提示词、开发者提示词、用户输入、历史消息的优先级如何理解？
3. 多轮对话时上下文应该按什么顺序组织？
4. 工具结果进入上下文时应该保留原始输出还是结构化摘要？
5. 检索结果放入上下文时应该如何做去重、排序和裁剪？
6. 为什么“上下文接口怎么设计”也是常见面试题？
7. 上下文构造层应该放在应用层、编排层还是模型调用层？
8. 如何设计一个可观测、可调试的上下文拼装模块？
9. 上下文构造和 Prompt 模板的边界在哪里？
10. 当多个模块都想往上下文里写内容时，如何保证可控性？

## 4. 多轮对话与历史管理

1. 多轮对话时上下文应该如何管理？
2. 对话历史应该全部保留、窗口滑动，还是分层存储？
3. 为什么多轮对话里容易出现话题漂移？
4. 多轮对话中如何判断一段历史是否仍然相关？
5. 如何在多轮对话中识别用户约束是否被覆盖？
6. 多轮状态应该怎么表示？
7. 对话状态和业务状态为什么不能混在一份历史里？
8. 多轮任务执行时，如何管理中间变量、工具结果和最终结论？
9. 任务中断、续聊、切话题时，状态怎么迁移？
10. 客服、Copilot、搜索问答、工作流助手这几类应用的多轮历史管理有什么区别？

## 5. 上下文裁剪、压缩与摘要

1. 上下文裁剪怎么做？
2. 上下文压缩怎么做？
3. 历史摘要怎么做？
4. 滑动窗口、摘要记忆、结构化状态提取分别适合什么场景？
5. 为什么直接截断历史往往会伤害效果？
6. 裁剪时怎么避免把关键约束裁掉？
7. 压缩后的上下文如何验证信息没有失真？
8. 摘要记忆和原始轨迹并存时，应该怎么协同？
9. 长任务执行过程中，什么时候触发压缩最合适？
10. 上下文压缩失败后通常会出现什么症状？
11. 前端侧如何应对超长输入导致的上下文超限问题？
12. 当用户上传超长文档时，应该如何处理上下文超限？

## 6. 长上下文与 Long Context

1. 长上下文能力在应用层到底能解决什么问题？
2. 长上下文模型的实际效果为什么常常低于想象？
3. “Lost in the Middle” 是什么？
4. 为什么重要信息放在中间位置容易被模型忽略？
5. 长上下文场景下，如何设计信息排布顺序？
6. 长上下文什么时候应该配合检索，而不是直接全塞进去？
7. 长上下文和 RAG 的关系是什么？
8. 长上下文和摘要压缩的关系是什么？
9. 长上下文模型上线后，为什么仍然需要做上下文治理？
10. 长上下文问题通常应该从哪些层次来拆？
11. 长上下文能力、上下文窗口管理、推理层缓存优化三者是什么关系？

## 7. 记忆系统设计

1. Agent 的记忆系统怎么设计？
2. 长期记忆和短期记忆分别怎么设计？
3. 混合记忆架构应该怎么设计？
4. 用户画像记忆、偏好记忆、事实记忆、任务记忆应该如何拆分？
5. 哪些信息适合写入长期记忆，哪些信息不应该写？
6. 记忆写入应该同步做还是异步做？
7. 记忆写入的触发条件应该怎么定？
8. 记忆存储层应该用关系库、KV、向量库，还是混合存储？
9. 记忆的主键、时间戳、作用域应该如何设计？
10. 一个面向生产环境的 memory store 至少要包含哪些字段？
11. 记忆系统为什么常常需要结构化存储和向量检索同时存在？
12. Agent memory 和用户 profile store 有什么区别？

## 8. 长期记忆的写入、召回与更新

1. 长期记忆如何写入？
2. 长期记忆如何召回？
3. 长期记忆如何更新？
4. 长期记忆如何删除？
5. 长期记忆如何淘汰？
6. 记忆衰退机制应该怎么做？
7. 如何避免旧信息干扰新任务？
8. 记忆覆盖问题如何解决？
9. 冲突记忆怎么处理？
10. 用户偏好变化后，旧记忆如何修正？
11. 召回长期记忆时，如何控制误召回？
12. 历史数据很大时，如何优化长期记忆查询效率？
13. 记忆检索为什么也会出现“召回有了但答案反而变差”？
14. 如何判断一段记忆是否已经过期？

## 9. 外部记忆、RAG 与 Memory Search

1. 记忆和 RAG 的边界是什么？
2. 外部记忆机制怎么接？
3. `memory_search` 怎么实现？
4. 什么时候应该用记忆检索，什么时候应该用知识库检索？
5. 用户私有记忆和公共知识库应该如何区分？
6. 记忆检索和文档检索可以共用一套召回链路吗？
7. 为什么很多 Agent 同时需要“会话记忆 + 用户记忆 + RAG 知识库”三套机制？
8. 外部记忆进入上下文前是否也要做重排和裁剪？
9. 记忆召回命中后，应该如何与当前 query 融合？
10. 记忆检索如何避免泄露其他用户的信息？

## 10. Session、线程隔离与并发安全

1. 多用户并发下 memory 如何做隔离？
2. LangChain 的 memory 在多用户并发下如何保证线程安全？
3. 为什么客服类场景容易出现串台问题？
4. 会话级状态、用户级状态、任务级状态应该如何隔离？
5. 一个用户同时开多个会话时，记忆该如何路由？
6. 多设备同步会话历史时，一致性怎么保证？
7. 多轮对话状态如何在高并发下保证一致性？
8. 什么时候需要乐观锁、版本号或事件流来管理状态？
9. 如果同一条记忆被多个流程同时更新，如何避免覆盖？
10. Agent 执行链中，状态写回失败怎么补偿？

## 11. KV Cache、Prefix Cache 与上下文复用

1. `KV Cache` 是什么？
2. `KV Cache` 为什么能提升推理速度？
3. `KV Cache` 节省的是哪一部分计算？
4. `KV Cache` 和“上下文记忆”是一个东西吗？
5. 为什么 `KV Cache` 和应用层 memory 不能混为一谈？
6. `KV Cache` 属于模型推理层优化，还是应用层状态管理？
7. `KV Cache` 会不会影响模型回答效果？
8. `KV Cache` 的代价是什么？
9. 长上下文场景下，`KV Cache` 为什么尤其关键？
10. 多轮对话里 `KV Cache` 能直接复用到什么程度？
11. 什么情况下 `KV Cache` 不能复用？
12. `prefix cache` 是什么？
13. `prefix cache` 和 `KV Cache` 的关系是什么？
14. 哪些场景特别适合做 `prefix cache`？
15. 为什么系统提示词、固定前缀、模板化上下文适合做前缀复用？
16. `prefix cache` 对多租户系统有什么挑战？
17. `KV Cache`、`prefix cache`、会话历史持久化三者分别解决什么问题？
18. `KV Cache` 的工作机制通常还会继续追到哪些问题？
19. 为什么 `KV Cache` 往往会继续追到上下文工程与应用层状态设计？
20. 做 Agent 或 Copilot 时，如何同时考虑 `KV Cache` 与上层上下文裁剪策略？

## 12. Skill、Tool 调用结果与上下文污染

1. skill 被 Agent 使用后是否占用上下文？
2. 工具调用结果为什么容易污染上下文？
3. 工具返回的长文本应该怎么进入上下文？
4. 工具调用中间过程应该全部保留还是摘要化？
5. ReAct 轨迹是否应该长期保留在上下文里？
6. Tool message、observation、final answer 应该如何分层保留？
7. 多工具串行调用时，如何避免上下文越来越脏？
8. 工具错误日志应不应该进入模型上下文？
9. 长任务场景下，工具轨迹如何归档为记忆或状态？

## 13. 前端、后端与工程落地

1. 前端如何管理多轮上下文？
2. 前端需要保存哪些状态，后端需要保存哪些状态？
3. 为什么很多上下文问题最终会落到后端状态管理设计上？
4. 网关层、应用层、Agent 编排层分别该承担哪些上下文职责？
5. 流式输出场景下，上下文是在请求开始前确定，还是边生成边调整？
6. 长任务、异步任务、恢复执行场景下，状态如何落盘？
7. 当上下文构造逻辑变复杂后，如何做观测和排障？
8. 如何设计上下文调试日志，既能排障又不泄露隐私？
9. 上下文和记忆模块上线后，应该监控哪些指标？

## 14. 评测、问题定位与 Bad Case

1. 如何评估上下文工程是否真的有效？
2. 如何评估记忆系统是否真的提升了效果？
3. 记忆命中率高，但回答效果差，可能是什么原因？
4. 上下文压缩后效果变差，应该从哪些维度排查？
5. 如何定位是“记忆没召回”，还是“召回了但没被模型利用”？
6. 如何做长上下文场景的 bad case 分析？
7. 如何判断模型是因为上下文过长而退化，而不是因为知识本身不足？
8. 如何设计一组能覆盖多轮、长任务、历史续接的评测集？
9. 当回答里出现串台、遗忘、约束丢失时，应该优先检查哪一层？

## 15. 高频综合题与项目追问

1. 如果让你设计一个带长期记忆的 Agent，你会怎么做？
2. 如果用户历史很长，但当前请求实时性要求很高，你会怎么折中？
3. 如果线上成本很紧，长上下文、RAG、记忆三者怎么取舍？
4. 如果一个 Copilot 经常忘记用户之前的约束，你会怎么排查？
5. 如果一个客服 Agent 容易串台，你会从哪几层下手？
6. 如果一个多轮 Agent 经常因为历史太长而超限，你会怎么治理？
7. 如果一个系统既要快、又要记住用户、还要避免上下文污染，你会怎么设计？
8. “上下文工程”“记忆系统”“推理层缓存优化” 三者的边界分别是什么？
9. 如果让你结合项目讲一次上下文工程优化，你会从哪些方面展开？
10. 如果让你结合项目讲一次记忆系统设计，你会从哪些方面展开？
