# Agent

## 1. 什么是 Agent？

1. 什么是 Agent？
2. Agent 和普通 ChatBot 的本质区别是什么？
3. Agent 为什么不只是“能聊天的大模型”？
4. Agent 为什么强调感知、决策、执行和反馈闭环？
5. Agent 的“自主性”到底体现在哪里？
6. Agent 的核心价值通常可以概括为哪几个方面？

## 2. 为什么需要 Agent？

1. 为什么会出现 Agent 这种应用形态？
2. Agent 想解决的核心问题是什么？
3. 什么任务用 Agent 明显比普通问答更合适？
4. 什么任务其实不适合用 Agent？
5. 为什么不是所有 AI 应用都应该做成 Agent？
6. 什么业务条件下采用 Agent 才是合理的？

## 3. Agent 当前的能力边界在哪里？

1. 当前 Agent 的能力边界主要体现在哪些方面？
2. 为什么很多 Agent 在复杂长任务上仍然不稳定？
3. 当前 Agent 的理想预期和真实落地之间主要差距在哪里？
4. 从模型侧看，哪些能力短板最限制 Agent 落地？
5. 从系统侧看，哪些工程短板最限制 Agent 落地？
6. 如果要让 Agent 变得更可靠，通常最值得优先改哪些地方？

## 4. Agent 和 Workflow 到底怎么区分？

1. Agent 和 Workflow 的区别是什么？
2. 什么场景更适合 Workflow？
3. 什么场景更适合 Agent？
4. 一个系统里能不能同时有 Workflow 和 Agent？
5. Workflow 什么时候会演化成 Agent？
6. 为什么很多业务最后落成“Workflow 为主，Agent 为辅”？
7. 什么情况下一个系统更应该被归类为 Workflow，而不是 Agent？

## 5. 一个 Agent 最小要长什么样？

1. 一个最小可用 Agent 至少需要哪些模块？
2. Planner、Executor、Tool、Memory、State、Evaluator 分别负责什么？
3. Observation 在 Agent 体系里起什么作用？
4. 为什么 Agent 一般不能只有“模型 + 工具”这两层？
5. Guardrail 应该放在 Agent 的哪一层？
6. Tool 层、状态层、编排层的边界应该怎么划？

## 6. Agent 是怎么跑起来的？

1. 一个 Agent 从接到请求到返回结果，完整链路是什么？
2. Agent 为什么要把“生成答案”和“选择动作”分开看？
3. Agent 每一步决策时，通常依赖哪些输入？
4. Agent 如何判断当前应该继续思考、调用工具，还是直接结束？
5. Agent 的 stop condition 一般怎么设计？
6. 为什么很多 Agent 最后都要显式状态机，而不能完全交给模型自由发挥？

## 7. Agent 为什么容易失控？

1. Agent 为什么容易任务漂移？
2. Agent 为什么容易重复思考、重复调用、重复试错？
3. Agent 为什么会误调工具、漏调工具，甚至死循环？
4. Agent 为什么会出现“每一步都像对的，但最终结果是错的”？
5. Agent 为什么 demo 看起来聪明，线上却很脆？
6. Agent 不收敛时，通常是哪几层出了问题？

## 8. Agent 常见模式到底在解决什么？

1. ReAct 是什么？
2. Plan-and-Execute 是什么？
3. Reflexion 是什么？
4. Self-Refine 这一类模式在解决什么问题？
5. ReAct、Plan-and-Execute、Reflexion 分别适合什么任务？
6. 为什么不是所有任务都需要显式规划？
7. 为什么有些任务用 ReAct 反而会变差？

## 9. 单 Agent 和多 Agent 怎么选？

1. 单 Agent 和多 Agent 应该怎么选？
2. 什么情况下值得上多 Agent？
3. 多 Agent 相比单 Agent 的收益是什么？
4. 多 Agent 相比单 Agent 的代价是什么？
5. 多 Agent 为什么容易带来成本膨胀和链路失控？
6. 在什么情况下坚持单 Agent 会更合理？

## 10. Agent 和 Tool、Memory、RAG 的关系是什么？

1. Agent 和 Tool Calling 的关系是什么？
2. Agent 和 Memory 的关系是什么？
3. Agent 和 RAG 的关系是什么？
4. 为什么很多 Agent 项目本质上是 `RAG + Tool + State` 的组合系统？
5. 什么时候 Agent 必须接工具？
6. 什么时候 Agent 必须接记忆？
7. 什么时候 Agent 必须接 RAG？
8. Tool、Memory、RAG 三者分别在解决什么问题？

## 11. Agent 的动作空间为什么必须受约束？

1. 什么叫 Agent 的动作空间？
2. 为什么 Agent 的动作空间必须收敛？
3. 工具为什么不能无限开放给 Agent？
4. 动作空间太大时，会带来什么问题？
5. 如何理解“Agent 的自由度越高，系统越不稳定”？
6. 参数约束、权限控制、工具白名单分别在限制什么？

## 12. Agent 最常见的线上问题有哪些？

1. Agent 哪些模块最容易在线上出问题？
2. 如何判断问题出在模型、提示词、工具、状态管理还是编排逻辑？
3. Agent 给出的建议和实际不符时，应该优先怎么排查？
4. 工具超时、返回脏数据或网络抖动时，Agent 应该怎么兜底？
5. 哪些 Agent 事故是最常见、最致命的？
6. 为什么 Agent 系统比普通问答系统更需要回放能力？

## 13. Agent 应该怎么评测？

1. Agent 为什么比单轮问答更难评估？
2. Agent 应该怎么做评测？
3. Agent 的评测为什么不能只看最终答案对不对？
4. 工具调用成功率和任务成功率为什么不能混为一谈？
5. Agent 的轨迹质量应该怎么评？
6. Agent 的规划能力应该怎么评？
7. 如何做 Agent 的 bad case 分类和闭环？

## 14. Agent 应该怎么工程化？

1. Agent 为什么更需要 trace、回放、状态快照和可观测性？
2. Agent 的核心线上指标通常有哪些？
3. 如何在效果、成本、时延之间做 Agent 的工程折中？
4. 为什么很多 Agent 最终都要加限流、超时、重试、熔断和降级？
5. 业务要求高并发、低时延、低成本时，Agent 该优先砍掉哪些能力？
6. 如何判断一个 Agent 系统是“更聪明了”，还是“只是更贵了”？

## 15. Agent 为什么会走向平台化？

1. Agent 中台和普通 AI 应用后端的区别是什么？
2. 为什么很多公司会做 Agent 平台，而不是让业务各自手写？
3. 一个 Agent 平台通常需要哪些核心能力？
4. 为什么 Agent 平台往往需要工作流编排能力？
5. 为什么 Agent 平台常常需要可视化画布？
6. 设计一个 Agent 工作流编辑器时，最难的点是什么？
7. 平台层如何支持多模型、多工具、多租户、多版本？
8. 常见 Agent 框架之间通常应该从哪些维度做比较？
9. Modular Agent 通常在解决什么问题？

## 16. Agent 项目深挖

1. 什么样的业务场景适合做 Agent？
2. 如果不用 Agent，改做 Workflow，会损失什么能力？
3. Agent 架构通常应该怎么设计？
4. Agent 项目里通常会负责哪一层？
5. Agent 框架通常为什么会选成当前方案？
6. 线上最难处理的 Agent bad case 是什么？
7. 从零设计一个企业级 Agent 系统时，通常应该先从哪些模块开始？
8. 如果重做当前 Agent 项目，通常最值得优先改哪一层？
