# Agent

## 1. Agent 的定义与边界

1. 什么是 Agent？
2. Agent 和普通 ChatBot、AI 助手、脚本增强型应用的本质区别是什么？
3. 为什么 Agent 不是“能聊天的大模型”，而是“围绕目标持续决策和执行的系统”？
4. Agent 的核心闭环为什么一定是感知、决策、执行、反馈，而不是单次生成？
5. 怎么判断一个系统本质上是 Agent，而不是包装成 Agent 的 Workflow？

## 2. 为什么需要 Agent

1. 为什么会出现 Agent 这种应用形态？
2. Agent 想解决的核心问题到底是什么？
3. 什么任务天然更适合 Agent，而不是普通问答？
4. 什么任务其实不适合 Agent？
5. 为什么不是所有 AI 应用都应该做成 Agent？

## 3. Agent 的能力边界与落地现实

1. 当前 Agent 的能力边界主要体现在哪些方面？
2. 为什么很多 Agent 在复杂长任务、多约束任务上仍然不稳定？
3. 当前 Agent 的理想预期和真实落地之间主要差距在哪里？
4. 从模型侧和系统侧看，最限制 Agent 落地的短板分别是什么？
5. 如果要让 Agent 真正变得更可靠，通常最值得优先改哪几层？

## 4. Agent、Workflow 与规则编排的关系

1. Agent 和 Workflow 的区别是什么？
2. 什么场景更适合 Workflow，什么场景更适合 Agent？
3. 一个系统里为什么经常会同时存在 Workflow 和 Agent？
4. Workflow 什么时候会自然演化成 Agent？
5. 为什么很多业务最后会落成“Workflow 为主，Agent 为辅”？

## 5. Agent 的最小架构

1. 如果基于 LangChain / LangGraph 搭一个最小可用 Agent，最小闭环至少需要哪些模块？
2. 这些模块在 LangChain / LangGraph 里通常分别由什么抽象承载？
3. 在一个最小可运行 Agent 里，哪些是最小必需，哪些属于增强项？
4. Observation 在 Agent 体系里为什么是关键输入？
5. 为什么真实 Agent 通常不能只有“模型 + 工具”这两层？
6. Tool 层、状态层、编排层、Guardrail 层的边界应该怎么划？

## 6. Agent 的运行链路

1. 如果基于 LangGraph 把一个 Agent 真正跑起来，从接到请求到返回结果，完整链路通常是什么？
2. 为什么 Agent 要把“生成答案”和“选择动作”拆开看？
3. Agent 每一步决策时，通常依赖哪些输入？
4. Agent 如何判断当前应该继续思考、调用工具，还是直接结束？
5. stop condition 一般应该怎么设计？
6. 为什么很多 Agent 最后都要落成显式状态机，而不能完全交给模型自由发挥？

## 7. Agent 为什么容易失控

1. Agent 为什么容易任务漂移、重复调用、重复试错，甚至死循环？
2. 为什么 Agent 会出现“每一步看起来都对，但最终结果是错的”？
3. 为什么很多 Agent demo 很聪明，线上却很脆？
4. Agent 不收敛时，通常是哪几层同时出了问题？
5. 如果要系统性地解释 Agent 失控，应该从哪些根因层来拆？

## 8. Agent 的常见模式

1. ReAct、Plan-and-Execute、Reflexion、Self-Refine 这几类模式分别在解决什么问题？
2. 这些模式分别适合什么任务，不适合什么任务？
3. 为什么不是所有任务都值得上显式规划？
4. 为什么有些任务用 ReAct 反而会变差？
5. 如果让你在 LangGraph 里落一个真实业务 Agent，通常应该怎么选模式，而不是把 ReAct 当默认答案？

## 9. 单 Agent 与多 Agent

1. 单 Agent 和多 Agent 应该怎么选？
2. 什么情况下值得上多 Agent？
3. 多 Agent 相比单 Agent 的核心收益和核心代价分别是什么？
4. 多 Agent 为什么特别容易带来成本膨胀和链路失控？
5. 在什么情况下坚持单 Agent 反而是更强的工程判断，而不是“能力做不到多 Agent”？

## 10. Agent 和 Tool、Memory、RAG 的关系

1. Agent 和 Tool Calling、Memory、RAG 分别是什么关系？
2. 为什么很多 Agent 项目本质上是 `RAG + Tool + State` 的组合系统？
3. 什么情况下 Agent 必须接工具？
4. 什么情况下 Agent 必须接记忆？
5. 什么情况下 Agent 必须接 RAG？
6. Tool、Memory、RAG 三者分别在解决什么问题，边界又该怎么划？
7. 如果基于 LangChain / LangGraph 落地，这三层通常应该怎么接，而不是互相混在一起？

## 11. 动作空间与系统约束

1. 什么叫 Agent 的动作空间？
2. 为什么 Agent 的动作空间必须受约束，而不能无限开放？
3. 动作空间过大时，通常会带来哪些系统性问题？
4. 如何理解“Agent 的自由度越高，系统越不稳定”？
5. 参数约束、权限控制、工具白名单、显式编排分别在限制什么？
6. 如果你用 LangGraph 或自定义编排框架实现，一个可控的动作空间通常该怎么收紧？

## 12. 线上问题、评测与工程化

1. Agent 系统最常见的线上问题通常有哪些？
2. 如何判断问题出在模型、提示词、工具、状态管理还是编排逻辑？
3. 为什么 Agent 系统比普通问答系统更需要 trace、回放、状态快照和可观测性？
4. Agent 为什么比单轮问答更难评测，评测时通常要看哪些层？
5. 工具调用成功率、轨迹质量、规划能力、任务成功率之间应该怎么一起看？
6. 如何在效果、成本、时延之间做 Agent 的工程折中？

## 13. 平台化与框架选型

1. Agent 为什么会走向平台化，而不是继续让业务各自手写？
2. Agent 中台和普通 AI 应用后端的区别是什么？
3. 一个 Agent 平台通常需要哪些核心能力？
4. 为什么 Agent 平台往往同时需要工作流编排能力和可视化画布？
5. LangChain、LangGraph、AutoGen、CrewAI 这类 Agent 框架通常应该从哪些维度做比较？
6. 如果项目最后没有继续重度依赖框架，而是逐步下沉自研，通常是因为什么？
7. Modular Agent 这类设计通常在解决什么问题？

## 14. Agent 项目深挖

1. 什么样的业务场景适合做 Agent？
2. 如果不用 Agent，改做 Workflow，会损失什么能力，又会换来什么好处？
3. Agent 架构通常应该怎么设计？
4. 如果让你结合一个真实项目讲 Agent 架构，最值得展开讲的模块通常有哪些？
5. 为什么会选成当前框架、当前模式、当前工具体系？
6. 线上最难处理的 Agent bad case 通常是什么？
7. 从零设计一个企业级 Agent 系统时，应该先从哪些模块开始？
8. 如果重做一次当前 Agent 系统，通常最值得优先改哪一层？
