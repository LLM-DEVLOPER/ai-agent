# 模型基础、Transformer、微调与对齐

## 1. 应用开发为什么也要懂模型基础

1. AI 应用开发通常需要掌握哪些模型基础？
2. Transformer、LoRA、RLHF 这些知识在应用开发里分别能帮助理解什么？
3. 模型基础理解在应用开发里到底能帮你解决什么问题？
4. 什么程度的模型基础算“够用”？
5. 应用开发中，哪些问题需要回到模型层面分析？

## 2. Transformer 与基本结构

1. Transformer 的核心结构是什么？
2. 为什么 Transformer 能成为大模型时代的基础架构？
3. Self-Attention 在解决什么问题？
4. 为什么需要位置编码？
5. Feed-Forward 层在 Transformer 里起什么作用？
6. Encoder-only、Decoder-only、Encoder-Decoder 的区别是什么？

## 3. Attention 机制与结构变体

1. Attention 的基本计算过程是什么？
2. Query、Key、Value 应该怎么理解？
3. 为什么 Attention 里要做缩放？
4. Self-Attention 和 Cross-Attention 的区别是什么？
5. 为什么 Attention 机制既是能力来源，也是性能瓶颈？
6. MHA、MQA、GQA、MLA 分别是什么？
7. 为什么会从 MHA 继续演化出 MQA 和 GQA？
8. MQA、GQA、MLA 分别在解决什么问题，它们为什么会直接影响推理成本和显存占用？
9. 什么情况下应用开发也需要理解这些结构差异？

## 4. 开源模型认知与模型选型

1. 主流开源模型通常应该从哪些维度理解和比较？
2. Qwen 这类主流模型通常会被从哪些角度考？
3. MoE 在解决什么问题？
4. 长上下文能力在模型层意味着什么？
5. 为什么不是所有应用问题都靠换更大的模型解决？
6. 模型选型通常应该从哪些维度做 trade-off？

## 5. 采样、解码与输出行为

1. `top-k` 和 `top-p` 分别在控制什么？
2. 温度参数通常在控制什么？
3. 为什么采样策略会直接影响模型输出风格和稳定性？
4. 什么场景更适合偏确定性的解码，什么场景更适合保留随机性？
5. 为什么采样参数不只是推理问题，也会影响应用体验？

## 6. 微调到底在解决什么问题

1. 微调在解决什么问题？
2. 什么时候问题的本质是“需要微调”，而不是“需要 RAG / Prompt”？
3. SFT 通常在解决什么问题？
4. 微调为什么会带来能力增强，也可能带来遗忘？
5. 微调数据为什么会直接决定模型行为边界？
6. 什么情况下不值得做微调？

## 7. LoRA 与参数高效微调

1. LoRA 的核心思想是什么？
2. 为什么低秩分解可以显著降低微调成本？
3. LoRA 相比全量微调的主要优势是什么？
4. LoRA 的主要局限是什么？
5. LoRA 的关键超参数一般从哪些角度理解？
6. LoRA 效果不好时通常先查什么？
7. QLoRA、LoRA 变体一般在解决什么问题？

## 8. 训练数据、样本质量与 CoT 数据

1. 微调数据通常应该怎么构造？
2. 为什么训练样本质量往往比样本数量更重要？
3. 坏样本通常会把模型带向哪些问题？
4. 偏好对数据通常怎么构造？
5. 从用户行为日志抽训练数据时通常要注意什么？
6. CoT（Chain-of-Thought）训练数据通常是如何构造的？
7. CoT 数据和普通指令微调数据的核心区别是什么？
8. 什么任务更依赖推理轨迹数据而不是普通问答数据？
9. 为什么推理能力增强不等于知识能力增强？

## 9. 对齐、偏好优化与强化学习

1. RLHF 在解决什么问题？
2. DPO 在解决什么问题？
3. PPO 和 DPO 的核心区别是什么？
4. reference model 在对齐里起什么作用？
5. 为什么偏好优化更多是在“让模型更符合人类偏好”，而不是“补知识”？
6. DPO 相比 PPO 的优势和局限通常怎么讲？
7. 在线强化学习和离线强化学习的区别是什么？
8. 强化学习在 Agent 场景里通常被期待解决什么问题？
9. 为什么很多业务场景里并不会真的上重型 RL？

## 10. 训练、推理与系统设计的连接点

1. 训练阶段的设计为什么会影响线上推理效果？
2. 模型结构、量化、长上下文能力为什么会反过来影响应用设计？
3. SFT、DPO、RAG、Prompt 在系统里通常怎么分工？
4. 为什么模型能力上限和系统工程上限必须一起看？
5. 应用问题究竟该改模型还是改系统，通常该从哪些层次拆？

## 11. 模型基础与微调项目深挖

1. 为什么会选择这个模型？
2. 为什么会选择微调，而不是只做 RAG / Prompt？
3. 什么情况下会选择 LoRA？
4. 训练数据通常应该怎么构造和清洗？
5. 微调是否有效通常怎么评估？
6. 通用能力遗忘通常怎么判断和处理？
7. 什么情况下值得做 CoT 数据增强、偏好优化或对齐？
8. 如果重做一次模型侧方案，通常最值得优先改哪一层？
