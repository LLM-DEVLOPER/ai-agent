# 工具调用、Function Calling 与 MCP

## 1. 为什么 Agent 一定会走到工具调用

1. 为什么很多 Agent 最后一定会接工具？
2. 工具调用在 Agent 体系里到底解决什么问题？
3. 为什么“只靠模型内知识”通常不够？
4. 为什么工具调用可以被理解成把模型从“会说”变成“会做”？
5. 什么场景必须接工具，什么场景其实不一定需要工具？

## 2. Function Calling 的定义与本质

1. 什么是 Function Calling？
2. Function Calling 的核心思想是什么？
3. Function Calling 和普通文本生成的本质区别是什么？
4. 为什么 Function Calling 本质上是在做“结构化动作决策”？
5. Tool Calling、Function Calling、普通 API 调用三者的边界应该怎么理解？
6. Function Calling 这一层更偏模型能力，还是更偏工程能力？

## 3. Function Calling 的运行链路

1. Function Calling 的完整链路通常是什么？
2. 模型是怎么判断当前该不该调用工具的？
3. 模型是怎么在多个工具之间做选择的？
4. 模型是怎么生成工具参数的？
5. 工具返回结果后，为什么通常还需要再喂回模型？
6. 为什么一次工具调用往往不是链路终点，而只是一个中间动作？

## 4. MCP 的定义、定位与边界

1. 什么是 MCP？
2. MCP 想解决的核心问题是什么？
3. MCP 为什么会在 Agent 场景里变得重要？
4. MCP 和普通 HTTP API 的区别是什么？
5. MCP 和 Function Calling 的区别是什么？
6. 为什么很多系统不会直接裸调工具，而要引入 MCP？

## 5. MCP 到底在标准化什么

1. MCP 到底在标准化什么东西？
2. MCP server 在整个链路里扮演什么角色？
3. MCP 为什么不只是“工具集合”？
4. MCP 对能力发现、描述、调用、结果返回分别起什么作用？
5. MCP 为什么有利于工具能力复用和 Agent 与工具生态解耦？
6. MCP 的作用通常可以从哪几层理解？

## 6. MCP 不只是一套 Tools

1. MCP 为什么不只是一套 tools 协议？
2. MCP 里的 prompts、resources、completions、sampling、elicitation 分别在解决什么问题？
3. 为什么最近对 MCP 的理解，已经不能只停留在 tool calling？
4. MCP 的能力发现和传统接口目录、本地工具注册有什么本质区别？
5. 什么情况下项目里真的需要用到 MCP 的 prompts、resources、sampling 或 elicitation？

## 7. Skill 的抽象与作用

1. 什么是 Skill？
2. Skill 和 Tool 的区别是什么？
3. Skill 和 Function Calling 的区别是什么？
4. Skill 和 MCP 暴露的能力是什么关系？
5. 为什么很多系统会把能力先封装成 Skill，而不是直接暴露底层工具？
6. 什么场景更适合设计成 Skill，而不是裸工具？

## 8. Schema、Structured Output 与调用质量

1. 工具 schema 为什么会直接影响调用正确率？
2. 工具名、工具描述、参数名为什么都很关键？
3. 什么样的 schema 更容易让模型调用对？
4. required 字段、默认值、枚举约束分别在帮助什么？
5. Structured Output 和 Function Calling 的关系是什么？
6. JSON Schema 为什么会越来越重要？
7. `tool choice`、强制调用、禁止调用分别适合什么场景？

## 9. 误调、漏调与参数错误

1. 模型为什么会误调、漏调、重复调工具？
2. 模型为什么有时候明明该调工具，却硬答，或者明明该直接回答，却非要调工具？
3. 工具参数为什么这么容易出错？
4. 参数缺失、类型错误、字段错填通常是怎么产生的？
5. 这些问题通常是出在模型、Prompt、schema，还是编排逻辑？
6. 如果工具选择和参数生成都不稳定，优先该从哪几层治理？

## 10. 多工具编排与上下文处理

1. 多工具调度为什么比单工具调用复杂很多？
2. 串行调用和并行调用分别适合什么场景？
3. Router、Planner、Tool Executor 在多工具编排里分别负责什么？
4. 为什么工具一多，链路就容易失控？
5. 工具结果为什么不能直接原样无脑塞回上下文？
6. 工具返回长文本、结构化结果、报错信息时，通常应该怎么处理？
7. 为什么很多工具调用问题最后会继续演化成上下文工程问题？

## 11. 失败处理、稳定性与治理

1. 工具超时、失败、返回脏数据、权限错误时，通常应该怎么兜底？
2. 为什么工具失败不能简单让模型“自由发挥”？
3. 重试、回退、换工具、直接拒答分别适合什么情况？
4. 如何提升 Function Calling 的稳定性？
5. Prompt、schema、工具描述、训练数据分别在解决什么问题？
6. 什么时候值得为工具调用专门做训练或对齐？

## 12. Agent、RAG 与工具调用的关系

1. Function Calling、Skill 和 Agent 的关系是什么？
2. Function Calling 和 RAG 的关系是什么？
3. RAG 知识库链路里，什么情况下适合用 Function Calling 来接？
4. 为什么很多 RAG 系统不会把检索完全包进模型自由决策里？
5. Agent + Tool + Skill + RAG 的系统里，边界通常应该怎么划？
6. 什么时候工具调用只是执行层，什么时候它会反过来影响规划层？

## 13. 观测、评测与项目深挖

1. 工具调用系统为什么特别需要观测、回放和 trace？
2. 工具调用成功率、参数正确率、任务成功率为什么不能混在一起看？
3. 工具调用效果通常应该怎么评测？
4. 如何区分是“模型不会调”，还是“工具本身不好用”？
5. 一个工具调用项目里的链路通常应该怎么设计？
6. 什么情况下更适合用 Function Calling，什么情况下更适合手写规则编排？
7. 线上最难处理的工具调用 bad case 通常是什么？
8. 如果重做一次工具调用系统，通常最值得优先改哪一层？
