# 检索、召回、重排与向量库

## 1. 为什么检索层这么重要？

1. 为什么很多 RAG 系统的效果瓶颈其实在检索层？
2. 检索层在整个 RAG 链路里到底负责什么？
3. 为什么生成模型再强，也不能替代一个差的检索层？
4. 为什么 RAG 链路问题最后往往会继续追到检索层？
5. 检索层为什么决定了后面生成阶段能不能“言之有据”？
6. 检索层的核心目标通常如何定义？

## 2. 检索层的完整链路是什么？

1. 一个完整的检索链路通常包含哪些步骤？
2. 从用户 query 到最终进入模型上下文，中间要经过哪些阶段？
3. query 理解、召回、过滤、重排、截断、上下文拼装的边界分别是什么？
4. 为什么很多检索问题不是单点优化，而是整条链路协同优化？
5. 检索链路图通常应该怎么拆模块？
6. 检索层和生成层的边界应该怎么划？

## 3. 稀疏检索和稠密检索到底怎么理解？

1. 什么是稀疏检索？
2. 什么是稠密检索？
3. BM25 为什么属于稀疏检索？
4. 向量检索为什么属于稠密检索？
5. 稀疏检索和稠密检索各自最擅长解决什么问题？
6. 为什么关键词检索对专有名词、型号、术语更敏感？
7. 为什么向量检索更擅长语义相似和同义表达？
8. 什么时候只做关键词检索就够了？
9. 什么时候只做向量检索会出问题？

## 4. 为什么很多生产系统都会做混合检索？

1. 为什么很多生产级系统最后都会采用混合检索？
2. 混合检索到底在弥补什么问题？
3. 稀疏检索和稠密检索为什么很适合配合使用？
4. BM25 + 向量检索这种组合通常在解决什么问题？
5. 混合检索结果一般怎么融合？
6. 为什么融合排序本身也是一个关键问题？
7. 什么场景下混合检索的收益会特别明显？
8. 什么场景下做混合检索反而收益不大？

## 5. Embedding 模型为什么决定召回上限？

1. embedding 模型为什么会决定召回上限？
2. query 向量和文档向量为什么必须在同一个语义空间里？
3. 选择 embedding 模型时应该重点看哪些维度？
4. 通用 embedding 模型和领域 embedding 模型应该怎么选？
5. 为什么不能只看公开榜单决定 embedding 选型？
6. 什么情况下需要对 embedding 模型做微调？
7. embedding 微调通常是在解决哪些召回问题？
8. query-side 改写和 embedding-side 优化分别在解决什么问题？

## 6. 向量数据库到底在做什么？

1. 向量数据库在检索系统中到底负责什么？
2. 向量库和普通关系库的角色区别是什么？
3. 为什么很多项目不会直接拿 MySQL 硬做向量检索？
4. `pgvector`、`FAISS`、`Milvus`、`Qdrant`、`Weaviate` 这类方案大致怎么取舍？
5. 自建向量库和托管向量库各自适合什么场景？
6. 向量库独立部署和嵌在主库里，各自有什么 trade-off？
7. 向量库除了存向量，还应该存哪些元数据？
8. 一个生产可用的向量库表结构通常至少要包含哪些字段？

## 7. 向量索引为什么是性能关键？

1. 为什么向量索引会直接决定检索性能？
2. 没有索引时，向量检索为什么不可扩展？
3. `IVF` 和 `HNSW` 的核心区别是什么？
4. `IVF` 适合什么场景？
5. `HNSW` 适合什么场景？
6. 索引构建速度、查询延迟、召回效果通常怎么权衡？
7. 大规模索引为什么常常要在准确率和时延之间做 trade-off？
8. 索引参数为什么经常要结合业务数据调优？

## 8. 向量库的更新和维护为什么难？

1. 向量库为什么不是“一次建好就结束”？
2. 新文档新增时，索引应该怎么更新？
3. 文档更新时，旧 chunk 和新 chunk 应该怎么处理？
4. 文档删除时，索引和元数据如何保持一致？
5. 增量更新和全量重建分别适合什么情况？
6. 为什么大规模增量更新会成为工程难点？
7. 索引失效通常会有哪些表现？
8. 如何判断问题是出在数据没更新，还是索引没生效？

## 9. 检索结果为什么还需要过滤？

1. 为什么召回之后通常还要做过滤？
2. 元数据过滤通常在解决什么问题？
3. 权限过滤为什么是企业级检索系统的刚需？
4. 多租户场景下，知识库隔离一般放在哪一层实现？
5. 时间过滤、业务过滤、用户过滤分别在解决什么问题？
6. 为什么很多“检索不准”其实是过滤策略设计不合理？
7. 过滤过严和过滤过松分别会带来什么问题？

## 10. 为什么还要做重排？

1. 为什么召回之后通常还要做 rerank？
2. 召回层和重排层的职责边界是什么？
3. reranker 在解决什么问题？
4. 为什么 embedding 相似度高，不等于答案相关性一定高？
5. Cross-Encoder 类 reranker 为什么通常更准？
6. rerank 的代价是什么？
7. rerank 一般放在多大的候选集上做比较合适？
8. topk 和 topn 应该怎么定？
9. rerank 后为什么还需要再做截断？

## 11. 父子块、分层检索、多路召回在解决什么？

1. 父子块检索在解决什么问题？
2. small-to-big 这类策略为什么常见？
3. 为什么很多时候“检索更小块，返回更大块”会更稳？
4. 多路召回在解决什么问题？
5. query 改写、多 query 检索在解决什么问题？
6. 图检索、知识图谱检索更适合什么问题？
7. GraphRAG 和普通向量检索的边界是什么？
8. 图数据库、图计算框架、知识图谱存储方案之间通常应该怎么区分？

## 12. 检索效果差时应该怎么排查？

1. 如果出现漏召，应该优先排查哪些环节？
2. 如果召回了错误结果，应该优先排查哪些环节？
3. 如果召回结果相关，但最终答案还是不对，通常问题在哪一层？
4. 如何判断问题出在 query、本体数据、embedding、索引、过滤还是重排？
5. 为什么很多“召回不准”其实是 query 理解问题？
6. 为什么很多“效果波动”其实是数据更新和索引更新不同步？
7. 检索 bad case 一般应该怎么分类？

## 13. 检索系统应该怎么评测？

1. 检索系统应该怎么做评测？
2. 召回率、准确率、MRR、NDCG 分别适合看什么？
3. 为什么不能只看最终答案效果，而忽略检索层指标？
4. rerank 的效果应该怎么单独评测？
5. 如何构建一套自己的检索评测集？
6. 自动化评测和人工 spot check 应该怎么配合？
7. bad case 回流为什么对检索系统特别重要？

## 14. 检索系统的性能通常卡在哪？

1. 检索链路的性能瓶颈通常出在哪些地方？
2. 大规模检索场景下，时延一般会被哪些环节拉高？
3. 实时重排为什么很容易把 latency 拉爆？
4. 检索系统为什么经常要在召回效果和时延之间做权衡？
5. 大规模向量检索场景下，架构 trade-off 一般怎么做？
6. 当线上 latency 过高时，优先该砍哪一层？
7. 什么时候值得做缓存、分层索引或异步预计算？

## 15. 检索项目深挖

1. 检索项目里的链路通常应该怎么设计？
2. 向量库通常为什么会选成当前方案？
3. embedding 模型通常为什么会选成当前方案？
4. 什么时候项目里值得做混合检索？
5. 什么时候项目里值得做 rerank？
6. chunk 策略通常为什么会定成当前方案？
7. 权限过滤、多租户隔离和数据更新通常应该怎么处理？
8. 检索效果通常应该怎么评估？
9. 线上最难处理的检索 bad case 是什么？
10. 如果重做一次检索系统，通常最值得优先改哪一层？
