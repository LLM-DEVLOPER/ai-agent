# 检索、召回、重排与向量库

## 1. 检索层在 RAG 里的定位

1. 为什么很多 RAG 系统的效果瓶颈其实在检索层？
2. 检索层和生成层的边界应该怎么划？
3. 为什么很多检索问题不是单点优化，而是整条链路协同优化？

## 2. 稀疏检索、稠密检索与混合检索

1. BM25 和向量检索分别在解决什么问题？
2. 稀疏检索和稠密检索各自更适合什么场景？
3. 为什么很多生产系统最终都会走到混合检索？
4. 混合检索最大的价值和最难的点分别是什么？

## 3. Embedding 与召回上限

1. embedding 模型为什么会决定召回上限？
2. 选择 embedding 模型时通常应该重点看哪些维度？
3. 通用 embedding 和领域 embedding 应该怎么取舍？
4. embedding 微调和 query 改写分别在解决什么召回问题？

## 4. 向量数据库与索引结构

1. 向量数据库在检索系统中到底负责什么？
2. `pgvector`、`FAISS`、`Milvus`、`Qdrant` 这类方案大致应该怎么取舍？
3. `IVF` 和 `HNSW` 的核心区别是什么？
4. 索引构建速度、查询延迟、召回效果之间通常怎么权衡？

## 5. 过滤、更新与一致性

1. 元数据过滤通常在解决什么问题？
2. 权限过滤和多租户隔离为什么是企业级检索系统的刚需？
3. 增量更新和全量重建分别适合什么情况？
4. 文档更新和删除时，为什么一致性会成为工程难点？

## 6. 重排与高级检索策略

1. 为什么召回之后通常还要做 rerank？
2. Cross-Encoder 类 reranker 为什么通常更准？
3. 父子块检索、small-to-big 这类策略在解决什么问题？
4. 多 query 检索、多路召回、GraphRAG 分别更适合什么问题？

## 7. 检索层评测、排障与项目深挖

1. 如果出现漏召，通常应该优先排查哪些环节？
2. 如果召回结果相关，但最终答案还是不对，问题通常在哪一层？
3. 检索系统为什么不能只看最终答案，通常应该怎么做分层评测？
4. 召回率、MRR、NDCG 分别适合看什么？
5. 一个检索项目里的链路通常应该怎么设计？
6. 如果重做一次检索系统，通常最值得优先改哪一层？
