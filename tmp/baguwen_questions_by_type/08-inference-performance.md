# 推理部署、性能优化与线上稳定性

## 1. 为什么推理与性能会成为 AI 应用瓶颈？

1. 为什么很多 AI 应用的核心问题不是模型能力，而是推理成本和时延？
2. 端到端延迟通常由哪些环节组成？
3. 为什么“模型调用慢”往往只是表象？
4. 为什么线上场景里必须同时看效果、时延和成本？
5. 推理优化的核心目标通常如何定义？

## 2. 推理链路一般长什么样？

1. 一个大模型推理请求从进入系统到返回结果，完整链路是什么？
2. 网关、编排层、模型服务层、后处理层各自通常在做什么？
3. 为什么推理问题经常不是单点问题，而是链路协同问题？
4. 检索、工具调用、模型生成、流式输出一般各占什么比例？
5. 推理链路图通常应该怎么拆？

## 3. Prefill、Decode、Batch 和 Schedule 在推理里是什么关系？

1. `prefill` 和 `decode` 分别在做什么？
2. 为什么 `prefill` 和 `decode` 的性能瓶颈通常不一样？
3. 首 token 延迟为什么往往更受 `prefill` 阶段影响？
4. 总完成时延为什么往往更受 `decode` 阶段影响？
5. batch size 为什么会同时影响吞吐和时延？
6. 连续批处理和静态批处理分别适合什么场景？
7. 推理调度器通常在解决什么问题？
8. 为什么高并发推理系统里 schedule 设计会直接影响吞吐和稳定性？

## 4. 为什么很多系统会选 vLLM？

1. vLLM 在解决什么问题？
2. 为什么很多线上系统会选 vLLM，而不是自己直接起模型服务？
3. vLLM 的核心优势是什么？
4. vLLM 架构里最关键的设计点是什么？
5. vLLM 为什么更适合高并发推理场景？
6. vLLM 的局限一般在哪里？

## 5. KV Cache 到底在优化什么？

1. KV Cache 是什么？
2. KV Cache 为什么能显著降低推理成本？
3. KV Cache 节省的是哪一部分计算？
4. KV Cache 的收益主要体现在哪些场景？
5. KV Cache 会带来什么代价？
6. KV Cache 和 prefix cache 的关系是什么？
7. 为什么长上下文场景下 KV Cache 特别关键？

## 6. PagedAttention 和 FlashAttention 在解决什么？

1. PagedAttention 在解决什么问题？
2. FlashAttention 在解决什么问题？
3. PagedAttention 和 KV Cache 的关系是什么？
4. FlashAttention 为什么本质上是在优化 attention 计算？
5. FlashAttention v1 和 v2 的差异通常从哪些角度理解？
6. 为什么这些优化都和显存访问模式强相关？

## 7. 推理优化一般可以从哪几层下手？

1. 推理优化通常可以从哪几层切入？
2. 模型层优化通常包括哪些方向？
3. 服务层优化通常包括哪些方向？
4. 调度层优化通常包括哪些方向？
5. Prompt 裁剪为什么也属于性能优化？
6. 为什么很多性能优化要在效果和成本之间做 trade-off？

## 8. 量化和低比特部署在解决什么？

1. 量化在解决什么问题？
2. 低比特部署通常适合什么场景？
3. 为什么量化后会出现理解能力下降？
4. 量化收益一般体现在哪些维度？
5. 量化的代价一般体现在哪些维度？
6. 如果量化后精度掉太多，通常有哪些补偿思路？
7. 什么场景下不值得激进量化？

## 9. Streaming 为什么也会成为优化题？

1. 为什么流式输出不只是交互问题，也是性能问题？
2. Streaming 在用户体验上解决什么问题？
3. 为什么支持 Streaming 后，系统压力反而可能变大？
4. 如果 Streaming 输出延迟超标，通常该怎么折中？
5. 首 token 延迟和总完成时延为什么都要看？
6. 什么场景下宁可不流式，也不要强上 Streaming？

## 10. 线上性能应该怎么权衡？

1. 响应速度和推理精度之间通常怎么权衡？
2. 线上效果和资源消耗应该怎么平衡？
3. 模型路由在效果、成本、时延之间一般怎么取舍？
4. 什么情况下值得上更大的模型，什么情况下应该退到小模型？
5. 如果业务预算很紧，优先砍哪类能力最合理？
6. 为什么很多线上系统不是追求单点最优，而是全链路最稳？

## 11. 并发、限流、重试和熔断为什么都是必修课？

1. 为什么线上一定要做限流、超时、重试、熔断？
2. AI 服务为什么比普通接口更容易被并发打爆？
3. 分布式令牌桶、漏桶、滑动窗口分别适合什么场景？
4. 幂等和重试为什么在模型调用场景更敏感？
5. 熔断和降级在 AI 服务里一般怎么设计？
6. 如果某一步推理结果不可靠，通常怎么做兜底？

## 12. 线上稳定性和观测应该怎么做？

1. 模型服务上线后通常要监控哪些指标？
2. 为什么 P50、P95、P99 都必须看？
3. GPU 利用率、显存占用、排队长度分别在说明什么？
4. 线上问题定位为什么一定要配合 trace 和回放？
5. 如果线上模型延迟突然变高，优先该查哪些层？
6. 如果线上效果波动很大，但服务没报错，通常该怀疑什么？

## 13. 部署和运维通常会被怎么问？

1. 模型服务一般怎么部署？
2. Docker 和容器化在模型服务里主要解决什么问题？
3. 为什么 AI 服务特别需要灰度、回滚和版本隔离？
4. 多模型服务共存时，资源隔离一般怎么做？
5. 训练和推理为什么最好不要共用同一套资源策略？
6. Ray、K8s 这类调度框架在训练或推理系统里通常扮演什么角色？

## 14. 推理与性能项目深挖

1. 推理链路通常应该怎么设计？
2. 什么情况下会选择 vLLM？
3. 什么情况下值得做 KV Cache、PagedAttention、FlashAttention 相关优化？
4. 哪些推理优化通常最容易真正带来收益？
5. 效果、成本和时延通常怎么权衡？
6. 线上最难处理的性能问题通常是什么？
7. 限流、降级和兜底通常怎么做？
8. 如果重做一次推理服务，通常最值得优先改哪一层？
